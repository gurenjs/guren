---
"web": patch
---

The deploy-time bundle size check now prints the plugin's attribution report (largest sources by package) before judging the budget, so a regression names its cause in the deploy log.
