---
"@guren/cli": patch
---

Distinguish missing source directories from unreadable ones during file and module discovery. Report interrupted check and audit scans as failures in text and JSON instead of silently treating unreadable directories as empty.
