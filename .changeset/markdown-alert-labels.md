---
"@guren/plugin-markdown": minor
---

Add an `alertLabels` renderer option overriding the label text per alert type (i18n, or a different vocabulary — several types may share one label). Class names are unaffected, and omitted types keep their default label.
