{
  "name": "@guren/example-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch bin/serve.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "db:make": "bunx guren make:migration",
    "db:migrate": "bunx guren db:migrate",
    "db:seed": "bunx guren db:seed"
  },
  "dependencies": {
    "@guren/cli": "workspace:*",
    "@guren/core": "workspace:*",
    "@guren/orm": "workspace:*",
    "@guren/testing": "workspace:*",
    "drizzle-orm": "0.44.7",
    "postgres": "^3.4.3",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "drizzle-kit": "0.27.0",
    "typescript": "^5.0.0",
    "vitest": "^2.1.2"
  }
}
