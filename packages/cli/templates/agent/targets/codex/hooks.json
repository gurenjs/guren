{
  "description": "Guren agent harness: run `guren gate` when a turn ends with uncommitted changes.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bun \"$(git rev-parse --show-toplevel)/.codex/hooks/gate-on-stop.ts\" \"$(git rev-parse --show-toplevel)\"",
            "timeout": 300,
            "statusMessage": "guren gate"
          }
        ]
      }
    ]
  }
}
