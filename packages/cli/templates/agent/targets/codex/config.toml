# Project-scoped Codex configuration. Codex reads this file in trusted
# projects only; the MCP endpoint itself is dev-only (GUREN_MCP=1) and
# rejects requests from other machines.
[mcp_servers.guren]
url = "http://localhost:3333/_guren/mcp"
