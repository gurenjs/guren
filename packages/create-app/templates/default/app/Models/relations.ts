// Register model relationships here. Import this file once during boot so
// helpers like `hasMany`/`belongsTo` run after all models are defined.
//
// Example:
// import { Post } from './Post'
// import { User } from './User'
//
// User.hasMany('posts', Post, 'authorId', 'id')
// Post.belongsTo('author', User, 'authorId', 'id')

